# clockControl - Variables de entorno
# Copiar a .env y ajustar valores

# Modo de ejecucion: "all" (todos los relojes) o "single" (un reloj)
RUN_MODE=all

# Intervalo del cron en minutos (modo produccion)
CRON_INTERVAL=5

# Intervalo en segundos (modo alta frecuencia, reemplaza CRON_INTERVAL)
# LOOP_INTERVAL_SECONDS=10

# Solo necesarios si RUN_MODE=single
# CLOCK_IP=172.16.21.150
# CLOCK_PORT=4370
# CLOCK_PASSWORD=0
